services:
  helios2mqtt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: helios2mqtt
    restart: unless-stopped
    # Configuration must be provided via a .env file.
    # Copy .env.example to .env (cp .env.example .env) and fill in your settings before starting.
    env_file:
      - .env
